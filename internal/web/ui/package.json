{
  "name": "evolve-report-viewer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Interactive web report viewer for evolve eval results (embedded into the Go binary).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.29.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.5",
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
